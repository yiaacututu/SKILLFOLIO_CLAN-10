<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Skillfolio">

        <!-- LAUNCHER: ActivityListKegiatan — Hub utama portofolio -->
        <activity
            android:name=".ActivityListKegiatan"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Form input Pengalaman -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- Form input Project -->
        <activity
            android:name=".TambahProjectActivity"
            android:exported="false" />

        <!-- Detail Kegiatan / Pengalaman -->
        <activity
            android:name=".ActivityDetailKegiatan"
            android:exported="false" />

        <!-- Detail Project -->
        <activity
            android:name=".ActivityDetailProject"
            android:exported="false" />

    </application>



</manifest>